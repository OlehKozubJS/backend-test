const { saveData } = require("./saveData");
const { loadData } = require("./loadData");
const { applicationRun } = require("./applicationRun");

module.exports = { loadData, saveData, applicationRun };
